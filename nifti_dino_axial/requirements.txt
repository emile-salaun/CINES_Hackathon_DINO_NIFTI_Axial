# ─────────────────────────────────────────────────────────────────────────────
# nifti_dino_axial — Python 3.11 / PyTorch 2.4 / CUDA 11.8
#
# Targeting torch 2.4.0+cu118: newest PyTorch compatible with driver 510.73.05
# (CUDA 11.8 wheels run on any driver >= 450.80).
#
# F.scaled_dot_product_attention is available from torch >= 2.0 — this is the
# hard minimum for our attention patch in training/trainer.py.
#
# Quick setup:
#   bash setup_venv.sh          # from the repo root — creates venv_dino/
#
# Manual install order:
#   1. PyTorch:
#        pip install torch==2.4.0 torchvision==0.19.0 \
#            --index-url https://download.pytorch.org/whl/cu118
#   2. pip install -r nifti_dino_axial/requirements.txt
#   3. FlexiCT path registration (no setup.py — use .pth file):
#        SITE=$(python -c "import site; print(site.getsitepackages()[0])")
#        echo "$(realpath models_pretrained/flexiCT/FlexiCT)" > "$SITE/flexict.pth"
#      Or just: bash setup_venv.sh  (does this automatically)
# ─────────────────────────────────────────────────────────────────────────────

# ── PyTorch (installed separately — see above) ────────────────────────────────
torch==2.4.0
torchvision==0.19.0

# ── Numerics ──────────────────────────────────────────────────────────────────
# numpy 1.26+ requires Python >= 3.9; on Python 3.8 the ceiling is 1.24.x
numpy==1.24.4

# ── Medical imaging ───────────────────────────────────────────────────────────
nibabel==5.3.2
SimpleITK==2.5.2
monai==1.3.2

# ── Vision / ML ───────────────────────────────────────────────────────────────
pillow==10.4.0
einops==0.8.1
timm==1.0.26
scikit-image==0.21.0
scikit-learn==1.3.2
scipy==1.10.1

# ── Config / logging / IO ─────────────────────────────────────────────────────
pyyaml==6.0.2
tqdm==4.67.1
pandas==2.0.3
matplotlib==3.7.5

# ── Distributed training ──────────────────────────────────────────────────────
accelerate==0.34.0
